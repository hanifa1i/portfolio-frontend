import styles from "./Spine.module.css"

type Props = {
    state : string;
}
export default function Spine(){

    return (<>
        <div className={`${styles.coverStitching}`}>
        </div>
    </>)
}